import {funcion1, funcion2} from 'rutaDependencia1';
function F(props){
    return(
        <div>
        </div>
    );
};
F.propTypes = {
  a: PropTypes.bool,
  b: PropTypes.bool,
  c: PropTypes.bool,
  d: PropTypes.bool,
  e: PropTypes.bool,
  f: PropTypes.bool
};