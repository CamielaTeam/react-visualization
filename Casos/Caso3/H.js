import {funcion1, funcion2} from 'rutaDependencia1';
function H(props){
    return(
        <div>
        </div>
    );
};
H.propTypes = {
  a: PropTypes.bool,
  b: PropTypes.bool,
  c: PropTypes.bool,
  d: PropTypes.bool,
  e: PropTypes.bool,
  f: PropTypes.bool,
  g: PropTypes.bool,
  h: PropTypes.bool
};